---
name: video-asset-generator
description: 为口播、文章、课程和解说视频规划并生成高质量竖屏或横屏视觉素材；在动态图形、AI 生图、图文融合、真实素材和混合形式之间做导演式选择，处理长视频素材密度、字幕分工、商业字体风险、移动端安全区、HyperFrames/GSAP 实现、批量渲染与成片抽帧验收。用于“做视频素材、口播配画面、补 B-roll、生成动画、生图背景、信息图、长视频视觉节奏、HyperFrames 短片”等任务。
---

# 视频素材生成器

目标不是逐句做“会动的 PPT”，而是为整条视频建立视觉节奏：该解释时给证据，该震撼时给空间，该呼吸时少放信息。

创意保持高自由度；字体授权、可读性、渲染确定性和最终成片验收使用硬门槛。

## 0. 先过字体闸门

在写任何含中文字的画面前执行：

1. 搜索用户或项目本地的阿里巴巴普惠体文件，优先使用实际存在的 Regular、SemiBold、Bold、Heavy 等字重。
2. 把所用字体复制到当前素材项目的 `assets/fonts/`，通过相对路径 `@font-face` 自托管；不要引用用户目录绝对路径。
3. **若找不到阿里巴巴普惠体，立即明确警告用户，并暂停含文字最终成片的渲染。** 可以继续做无字生图、构图草案和动画骨架，但不得悄悄换字体后交付。
4. 只有在用户明确提供或批准另一款字体，且其商业使用与再分发权限已被确认时，才使用替代字体。
5. 不把系统自带字体、网页字体、名称里带“免费”的字体或搜索结果自动视为可商用。不要默认回退到微软雅黑、黑体、Arial 或其他授权状态未确认的字体。
6. 字体可用于成片不等于可随 Skill 再分发。通用 Skill 默认不内嵌字体文件。

可先运行：

```powershell
python 'scripts/preflight.py' '<素材项目目录>'
```

字体缺失是阻断项，不是普通提醒。

## 1. 快速建立素材地图

先阅读全文或字幕，按意义而非标点分段。每个候选只记录六项：

`位置 | 传播任务 | 媒介 | 文字策略 | 主动作 | 与前后镜头的差异`

传播任务只选一个主项：

- 记住结论：章节、金句、反常识判断；
- 看见差距：前后代、效率、规模、成本；
- 理解关系：因果、流程、路由、闭环、层级；
- 感受规模：并行、集群、数据洪流、城市、根系、产线；
- 感受速度：迭代、回流、增长、冲破；
- 建立情绪：悬念、震撼、危机、突破；
- 完成转场：为下一章换空间、色温或节奏。

不要让一段同时承担完整字幕、复杂解释、金句和情绪高潮。

### 长视频密度

- 默认每 20–40 秒安排一次有效视觉变化；以用户要求和口播节奏为准。
- 10 分钟视频通常先规划约 15–30 个可插入点，再根据素材强弱删减。
- 连续三段不得都使用“大字 + 卡片 + 网格”。高信息图之后安排生图、真实素材或无字呼吸镜头。
- 先复用色彩、字体和光线逻辑，再变化空间、运动方向、媒介和信息密度；统一不等于雷同。

## 2. 按信息本质选择媒介

### 动态图形

用于能被精确画出的关系：数值对比、比例、流程、因果链、负载、时间线、公式。图形必须提供口播没有直接“看见”的证据。

### AI 生图

用于空间、尺度、隐喻和情绪大于精确刻度的内容。遇到“数十万、万卡、巨型、无穷、根系、城市、浪潮、实验室、产线”等概念，必须评估电影感生图版本。

生图默认：

- 生成无字画面，不让模型书写中文、数字、Logo 或品牌界面；
- 为后期文字预留负空间，但不牺牲视觉主体；
- 需要模型选择时，优先使用 Nano Banana 系列；若环境只提供其他工具，使用可用的高质量图像模型；
- 记录最终提示词、画幅、用途和文件名；
- 宏大场景优先用纵深、重复、尺度参照、环境光和层次制造“哇”的感觉。

### 图文融合

图片负责世界与情绪，动画层负责关键词、标题或数字。背景镜头运动与文字动画必须分层，背景的缩放/位移不得带动文字。

### 真实素材

人物、产品界面、Logo、论文、发布会、机房和新闻事实优先使用经过核验的真实素材。概念图不得冒充事实证据。

### 多版本

同一知识点可保留图形解释版、电影氛围版和无字版。新增候选使用新文件名，不覆盖已可用版本。

更细的视觉选择与构图启发见 [visual-playbook.md](references/visual-playbook.md)。

## 3. 让动画文字与字幕分工

每段先选一种主要策略：

- **K｜关键词**：数字、节点名、短标签；正常保留字幕。
- **T｜标题**：章节、子标题、转折；该时段可用素材文字替代字幕。
- **Q｜金句**：真正值得完整记住的句子，最多两行；替代字幕。
- **N｜无文字**：生图、实景、隐喻、转场；保留字幕。

对比、结构、图表和机制类默认只留关键词与数字，不复述完整口播。标题、重点、金句只有在其本身就是视觉主角时才完整保留。

## 4. 先完成稳定画面，再设计运动

先把信息全部出现后的画面做对，再考虑如何进出。不要用动画 transform 承担静态居中。

### 竖屏安全区基线

- 1080×1920 时，关键文字默认使用 `left:162px; width:756px`，即左右各约 15%。
- 稳定阅读状态的顶部关键内容通常从 `y=240px` 以下开始；顶部区域可放背景、光线和非关键装饰。
- 动画路径可以短暂穿过灵动岛、状态栏或画面边缘；限制的是停驻的主要内容，不是运动轨迹。
- 下部为字幕、人物和平台按钮预留空间。素材文字替代字幕时才使用原字幕区域。
- 背景与装饰可以出血；关键文字、数字和关系节点必须留在稳定安全区。

### 文字与容器

- 计算的是容器内可用宽度，不只是画布宽度；扣除边框、圆环、卡片内边距和装饰占位。
- 中英数混排分别估算或实测。字体、字号、字重或字距变化后重新检查。
- 英文单词末字母、百分号、单位和斜体外扩最容易越界，必须看原尺寸帧。
- 圆环、光斑和线框默认置于文字下层；除非遮挡是明确设计意图。

## 5. 对比必须让差距可见

大数字是结论，对比结构是证据。明确对比至少表现基线、方向、差值三者中的两项。

可映射为高度、长度、面积、数量、密度、距离、速度、等待时间、亮度、连接数或吞吐流。竖屏优先考虑同一底部基线向上增长、沿纵深超越、上下接力或旧状态被新状态冲破，不必机械左右分栏。

验收问题：遮住数字后，观众是否仍能判断谁更大、变化往哪里走？若不能，重做结构。

## 6. 运动讲关系，不只负责出现

每段只设一个主动作，其他元素用呼吸、视差、光线或轻微反应辅助。

- 对比：从共同基线生长或让新值明确超越旧值；
- 因果：前项推动、碰撞或转化为后项；
- 流程：沿真实路径流动，避免无意义粒子；
- 瓶颈：让最慢节点拖住整体；
- 闭环：让结果或失败样本真实回流；
- 规模：使用纵深、重复、视差和镜头推进，不只放大字号；
- 金句：用阅读节奏揭示，不与字幕争夺注意力。

动作出现顺序必须服从口播顺序。背景可先建立，关键文字和结论不得提前剧透。

## 7. 高速批量生产流程

1. **资产盘点**：字幕/时长、画幅、已有图片、真实素材、字体、输出目录。
2. **字体闸门**：确认普惠体并自托管；缺失则警告并停止含字成片。
3. **素材地图**：一次性完成全片候选与媒介配比，先解决重复问题。
4. **先做两种代表镜头**：一个精确图形、一个生图融合；若用户已有认可风格，直接沿用，不重复审批。
5. **先静态后动画**：先验证最终状态、负空间、对比和文字宽度。
6. **批量实现**：共享基础样式与字体声明，但每段重新决定构图和主动作；不复制整套模板。
7. **分段渲染**：每个素材从 0 秒开始独立导出，避免把长时间轴空白一起输出。
8. **先自动检查，再抽帧检查**：不通过则只重渲染受影响段。
9. **交付最终目录**：历史错误版本与最终成片分开，提供镜头清单和使用位置。

### 参数化模板

先判断新文案是否匹配 [template-catalog.md](references/template-catalog.md)。匹配时复制对应 `assets/templates/<类型>/template.html` 和 `example.json`，只替换参数与项目素材；不得复制旧案例文案、品牌素材或全部场景源码。未匹配时按本 Skill 从零设计，成功后再评估是否新增模板。

模板使用 HyperFrames 原生 composition variables 和本地固定版本 GSAP。预览时在 Studio 修改参数，渲染时使用 `--variables-file config.json --strict-variables`。不得擅自切换动画运行时。

## 8. HyperFrames / GSAP

实现前读取 [hyperframes-rules.md](references/hyperframes-rules.md)。核心底线：

- 子段通常 6–10 秒、`data-start="0"`，独立 MP4 输出；
- 静态布局不用 `xPercent/yPercent`、`translate(-50%)` 或时间线 `set()` 承担核心定位；
- CSS 与 GSAP 不同时控制同一元素的基础 transform；
- 使用确定性值，不用 `Math.random()`；
- 明确动画 class，不依赖脆弱的 `nth-of-type`；
- 先 lint 到 0 errors / 0 warnings，再渲染；
- 修改字体、布局、时序或动画后重新渲染受影响片段。

## 9. 成片验收

必须检查最终 MP4，而不是只看浏览器或代码。

1. 运行 `scripts/preflight.py` 搜索字体与危险定位问题。
2. 运行 HyperFrames lint，要求 0 errors / 0 warnings。
3. 验证分辨率、时长、帧率、编码和音轨需求。
4. 每段至少抽取约 25%、50%、75% 三帧；快速运动再补入场结束、峰值和退场前帧。
5. 原尺寸检查：画布裁切、容器内溢出、圆环遮字、顶部停驻安全区、字幕冲突、主体与文字抢占、英文末字母和单位。
6. 大批量抽帧使用单视频或小批次、低线程方式；不要用一个超大 FFmpeg 多输入拼图命令，以免资源耗尽或触发系统崩溃弹窗。
7. 自动检查通过不等于视觉正确；视觉检查通过也不能替代真实编码文件检查。

完整验收表见 [quality-gates.md](references/quality-gates.md)。

## 10. 可分发与隐私检查

交付通用 Skill 或模板前：

- 搜索并删除用户名、绝对路径、客户名、私人域名、访问令牌、Cookie、内部服务器地址和任务原文；
- 不打包用户字体、人物照片、Logo、截图、生成结果或项目提示词，除非明确允许再分发；
- 示例只使用虚构名称和相对路径；
- 不在 Skill 中记录“某用户已确认某字体授权”，授权结论只属于具体项目；
- 打包前运行验证脚本，并检查归档文件列表。

## 11. 交付物

- 独立 MP4；
- 可编辑 composition；
- 经项目授权的本地字体与生成图片；
- 镜头清单：文件名、建议位置、文字策略、建议用法；
- 生图提示词记录；
- lint、媒体规格与关键帧检查结果。

最终逐段问：它在证明、解释还是营造情绪？遮住文字后关系是否仍成立？文字是否只在复读字幕？有没有本应生图的宏大空间被做成 PPT？稳定内容是否安全，而运动是否足够大胆？只有答案清晰才交付。
